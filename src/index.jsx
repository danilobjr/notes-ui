import React from 'react'
import ReactDOM from 'react-dom'
import { DocumentationApp } from './documentation'

const appContainer = document.getElementById('app');

ReactDOM.render(<DocumentationApp />, appContainer);
